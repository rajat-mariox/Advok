node_modules/
dist/
# Runtime data store stays local; the bundled public-domain dictionary ships with the code.
data/*
!data/legal_terms.json
.env
# Private keys (Firebase service account, Apple .p8)
keys/
*.p8
